version: '3.8'
services:
  validator-dashboard:
    build: .
    ports:
      - "8080:8080"
    environment:
      - BEACONCHA_API_KEY=${BEACONCHA_API_KEY}
      - CHAIN=${CHAIN:-mainnet}
    restart: unless-stopped
